# Tutorial: [Function or Method Name]

Generated by: scribe
Date: [YYYY-MM-DD]
Package: [package name] [version]
Function(s): `[function_name()]`

---

## Overview

[One paragraph describing what this function does and when to use it. Mention the statistical method it implements, its key assumptions, and the type of output it returns.]

---

## Method

[Brief non-technical description of the statistical method. Optionally include the estimator in LaTeX.]

$$
[equation if relevant]
$$

Key assumptions:

- [Assumption 1]
- [Assumption 2]

---

## Installation

```r
# Install from CRAN
install.packages("[package name]")

# Or from GitHub
# remotes::install_github("[user/repo]")

library([package name])
```

---

## Basic Usage

```r
# Minimal working example
[function_name](
  formula = y ~ x,
  data = mydata
)
```

---

## Detailed Example

### Data

```r
# Generate or load example data
set.seed(42)
n <- 200
x <- rnorm(n)
y <- 1.5 * x + rnorm(n)
df <- data.frame(y = y, x = x)
```

### Estimation

```r
fit <- [function_name](
  formula = y ~ x,
  data = df
)
```

### Output

```r
summary(fit)
```

Expected output:

```
[paste representative output here]
```

### Extracting Results

```r
# Coefficients
coef(fit)

# Standard errors
sqrt(diag(vcov(fit)))

# Confidence intervals
confint(fit)
```

---

## Options and Arguments

| Argument | Default | Description |
| --- | --- | --- |
| `formula` | — | Model formula |
| `data` | — | Data frame |
| `[arg3]` | `[default]` | [Description] |

---

## Edge Cases

### Small samples

```r
# What happens with n < 30
[example code]
```

### Missing data

```r
# How the function handles NAs
[example code]
```

---

## Common Errors

**Error:** `[error message]`
**Cause:** [explanation]
**Fix:** [how to resolve]

---

## References

- [Author, Year. *Title*. Journal. DOI.]
- Package vignette: `vignette("[vignette-name]", package = "[package name]")`
- Help page: `?[function_name]`
